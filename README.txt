HOW TO USE

Deep Q-Learning Model:
    Run the following command to train the model:
       python deep_q_learning.py

Tournament:
    Run the following command to run the tournament:
       python tourney.py
    The results will be output to the /results folder in json format.